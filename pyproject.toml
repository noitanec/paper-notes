[project]
name = "paper-notes"
version = "0.1.0"
description = "An easy way to take notes on papers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "loguru>=0.7.3",
]
